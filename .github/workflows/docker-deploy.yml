name: CI/CD for Node.js App

on:
  push:
    branches:
      - main

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    steps:
      # Step 1: Checkout the code
      - name: Checkout repository
        uses: actions/checkout@v3

      # Step 2: Log in to Docker Hub
      - name: Log in to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      # Step 3: Build and push the Docker image
      - name: Build and push Docker image
        uses: docker/build-push-action@v4
        with:
          context: .
          push: true
          tags: abkaur95/nodejs-container-app:latest

  deploy:
    runs-on: ubuntu-latest
    needs: build-and-push

    steps:
      - name: Deploy to Server
        run: |
          echo "Deploying container to production..."
          # Add SSH + docker pull/run commands here if you have a server
          # Example (pseudo):
          # ssh user@YOUR_SERVER "
          #   docker pull abkaur95/nodejs-container-app:latest &&
          #   docker rm -f node-demo || true &&
          #   docker run -d --name node-demo -p 80:3000 abkaur95/nodejs-container-app:latest
          # "
